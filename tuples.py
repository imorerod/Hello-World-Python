
# tuples are immutable (cannot be changed)
# tuples use () as opposed to [] for lists
# use-case for tuples: data you never want to change

# this(below) is a list of tuples
coordinates = [(4, 5), (6, 7), (4, 9)]
# cannot do this(below)
# coordinates[1] = 10
print(coordinates[0])





